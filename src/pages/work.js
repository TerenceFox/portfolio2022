import * as React from "react";
import Layout from "../components/layout";

// markup
const IndexPage = ({ location }) => {
  return <Layout location={location}>Hi. I'm the work page</Layout>;
};

export default IndexPage;
