import * as React from "react";
import Layout from "../components/layout";

// markup
const IndexPage = ({ location }) => {
  return <Layout location={location}>Hi. I'm the photography page</Layout>;
};

export default IndexPage;
